#!/usr/bin/env bash

OOP=../oop
CLASSES=../demo

. $OOP/oop
. $CLASSES/Point

Dot() {
    extends Point

    case $__method__ in

        print_of_dot)
            echo "dotprint (${this[x]}; ${this[y]})"
        ;;

        *)
            $Parent $@
        ;;

    esac
}

Dot

